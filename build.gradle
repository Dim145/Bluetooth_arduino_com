plugins {
    id 'java'
}

group 'org.example'
version '1.0-SNAPSHOT'

repositories {
    mavenCentral()
    flatDir { dirs 'resources' }
}

dependencies {
    testCompile group: 'junit', name: 'junit', version: '4.12'
    compile (name: 'bluecove-2.1.1', ext: 'jar')
}
